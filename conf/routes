# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET         /                    controllers.Application.index

# Map static resources from the /public folder to the /assets URL path
GET         /assets/*file        controllers.Assets.at(path="/public", file)

POST        /                    controllers.Application.update

GET         /createSchema        controllers.Application.createSchema

GET         /dropSchema          controllers.Application.dropSchema

GET         /addData             controllers.Application.addData

GET         /dumpData             controllers.Application.dumpData
